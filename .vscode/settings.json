{
  "editor.tabSize": 2,
  "protoc": {
    // "path": "/path/to/protoc",
    // "compile_on_save": false,
    "options": [
      "--proto_path=proto"
      // "--proto_path=protos/v2",
      // "--proto_path=${workspaceRoot}/proto",
      // "--proto_path=${env.GOPATH}/src",
      // "--java_out=gen/java"
    ]
  }
}
